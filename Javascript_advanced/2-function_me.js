#!/usr/bin/node
/* A script for welcome message.
*/

function welcomeMessage(fullName) {
    alert('Welcome ' + fullName)
}
const guillaume = 'Guillaume';
const alex = 'Alex';
const fred = 'Fred';

welcomeMessage(guillaume);
welcomeMessage(alex);
welcomeMessage(fred);